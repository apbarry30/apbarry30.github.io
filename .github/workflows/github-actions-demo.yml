Skip to content
Search or jump to…
Pull requests
Issues
Codespaces
Marketplace
Explore

@apbarry30
DaveAldon
/
Next.js-and-GitHub-Pages-Example
Public template
Fork your own copy of DaveAldon/Next.js-and-GitHub-Pages-Example
Code
Issues
Pull requests
Actions
Projects
Security
Insights
Next.js-and-GitHub-Pages-Example/.github/workflows/node.js.yml
@DaveAldon
DaveAldon Update node.js.yml
Latest commit 330d354 on Oct 24, 2022
 History
 1 contributor
33 lines (26 sloc)  987 Bytes


# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: GitHub Pages deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'

      - name: Installing my packages
        run: npm ci

      - name: Build my App
        run: npm run build && npm run export && touch ./out/.nojekyll

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: public # The branch the action should deploy to.
          FOLDER: out # The folder the action should deploy.
Footer
© 2023 GitHub, Inc.
Footer navigation
Terms
Privacy
Security
Status
Docs
Contact GitHub
Pricing
API
Training
Blog
About
